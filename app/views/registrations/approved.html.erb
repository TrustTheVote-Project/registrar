<h1>
  <% if current_user %>
    Thank you <%= current_user.name %>, you have approved the Voter Registration for <%= @registration.name %> with Tracking ID <%=@registration.id%>.
  <% else %>
    The Voter Registration for <%= @registration.abbreviated_name %> with Tracking ID <%=@registration.id%> has been approved.
  <% end %>
</h1>